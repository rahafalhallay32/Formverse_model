fastapi==0.115.7        # FastAPI framework
uvicorn==0.34.0         # ASGI server for FastAPI
#openai>=1.0.0           # OpenAI Python library (use >=1.0.0 to work with the latest API)
pydantic==2.10.6        # Data validation and settings management (for FastAPI)
python-dotenv==1.0.1    # To load environment variables from a .env file
openai